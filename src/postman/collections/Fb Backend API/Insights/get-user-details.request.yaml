$kind: http-request
name: Get User Details
method: POST
url: "{{base_url}}/api/v1/insights/user"
order: 3000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "access_token": "{{access_token}}",
      "fields": "id,name,picture"
    }
